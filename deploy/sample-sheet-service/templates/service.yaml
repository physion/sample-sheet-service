apiVersion: v1
kind: Service
metadata:
  name: {{ template "sample-sheet-service.fullname" . }}
  labels:
    app: {{ template "sample-sheet-service.name" . }}
    chart: {{ template "sample-sheet-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.externalPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "sample-sheet-service.name" . }}
    release: {{ .Release.Name }}
